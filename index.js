/**
 * @format
 */

import React from 'react';
import {AppRegistry} from 'react-native';
import App from './app/App';

const TodoApp = () => <App/>;
AppRegistry.registerComponent('test', () => TodoApp);
